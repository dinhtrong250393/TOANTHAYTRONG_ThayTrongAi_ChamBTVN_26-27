sed -i 's/export default function TeacherTextbookTab({/const teacherTextbookCache = { currentGrade: null as any, currentChapter: null as any, currentLesson: null as any };\nexport default function TeacherTextbookTab({/g' src/components/TeacherTextbookTab.tsx
sed -i 's/const \[currentGrade, setCurrentGrade\] = useState<any>(null);/const [currentGrade, setCurrentGrade] = useState<any>(teacherTextbookCache.currentGrade);/g' src/components/TeacherTextbookTab.tsx
sed -i 's/const \[currentChapter, setCurrentChapter\] = useState<any>(null);/const [currentChapter, setCurrentChapter] = useState<any>(teacherTextbookCache.currentChapter);/g' src/components/TeacherTextbookTab.tsx
sed -i 's/const \[currentLesson, setCurrentLesson\] = useState<any>(null);/const [currentLesson, setCurrentLesson] = useState<any>(teacherTextbookCache.currentLesson);/g' src/components/TeacherTextbookTab.tsx
sed -i '/const \[currentLesson, setCurrentLesson\] = useState/a \  useEffect(() => { teacherTextbookCache.currentGrade = currentGrade; teacherTextbookCache.currentChapter = currentChapter; teacherTextbookCache.currentLesson = currentLesson; }, [currentGrade, currentChapter, currentLesson]);' src/components/TeacherTextbookTab.tsx
